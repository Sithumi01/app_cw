name: water_tank_app
description: A Flutter dashboard example for water tank monitoring

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  percent_indicator: ^4.2.3  # Add this line
  firebase_core: ^2.10.0  # Add this line
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
Use the following DNS:
Preferred: 8.8.8.8
Alternate: 8.8.4.4